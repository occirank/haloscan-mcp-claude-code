{
  "name": "@occirank/haloscan-mcp-claude-code",
  "version": "1.0.5",
  "description": "MCP stdio server for Haloscan SEO API (Claude Code compatible)",
  "type": "module",
  "license": "MIT",
  "bin": "./build/index.js",
  "scripts": {
    "build": "tsc && node -e \"require('fs').chmodSync('build/index.js', 0o755)\""
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.7.0",
    "axios": "^1.6.0",
    "dotenv": "^16.3.1",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
